export const splitRightShortcutLabel = 'Ctrl+Option+Shift+Right';
export const splitDownShortcutLabel = 'Ctrl+Option+Shift+Down';

export function workspaceIndexShortcutLabel(index: number): string {
  return `Ctrl+Shift+${index}`;
}
